[project]
name = "without-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.136.3",
    "fastapi-cli>=0.0.24",
]

[tool.fastapi]
entrypoint = "src.main:app"
